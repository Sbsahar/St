# بوت إدارة المجموعات - النسخة المحسّنة

بوت Telegram متكامل لإدارة المجموعات، مستوحى من بوت **Group Help** الشهير، مع الحفاظ على جميع الميزات الأصلية وإضافة ميزات جديدة.

## ✨ الميزات الجديدة

### 1. لوحة تحكم متكاملة (`/settings`)
لوحة تحكم تفاعلية بالأزرار تسمح للمشرفين بإدارة جميع إعدادات المجموعة من مكان واحد:
- تفعيل/تعطيل فحص الميديا
- تفعيل/تعطيل حظر الروابط
- إدارة الكلمات المحظورة
- إدارة رسائل الترحيب
- إعدادات التحذيرات
- عرض إحصائيات المجموعة

### 2. نظام التحذيرات الذكي
- أمر `/warn` لتحذير المستخدمين
- أمر `/unwarn` لإزالة تحذير
- أمر `/warnings` لعرض عدد التحذيرات
- كتم تلقائي بعد 3 تحذيرات لمدة ساعة

### 3. أوامر إدارة محسّنة
جميع أوامر الإدارة تدعم طرق متعددة لتحديد المستخدم:
- الرد على رسالة المستخدم
- استخدام المعرف `@username`
- استخدام الآيدي الرقمي
- استخدام mention

### 4. بنية منظمة وقابلة للتطوير
تم إعادة هيكلة الكود بالكامل إلى وحدات متخصصة:
```
St-main/
├── ste_new.py           # الملف الرئيسي
├── config.py            # الإعدادات والمتغيرات
├── data_manager.py      # إدارة البيانات
└── bot_modules/
    ├── admin.py         # أوامر الإدارة
    ├── moderation.py    # الإشراف التلقائي
    ├── settings.py      # لوحة التحكم
    └── utils.py         # دوال مساعدة
```

## 🎯 الميزات المحفوظة من النسخة الأصلية

تم الحفاظ على جميع الميزات الأصلية للبوت:
- ✅ فحص الميديا (الصور والفيديوهات) للمحتوى غير اللائق
- ✅ نظام الكلمات المحظورة
- ✅ حظر الروابط
- ✅ رسائل الترحيب
- ✅ وحدة اليوتيوب
- ✅ وحدة القنوات
- ✅ التحميل من السوشيال ميديا
- ✅ جميع الأوامر الإدارية الأخرى

## 📋 قائمة الأوامر

### أوامر المشرفين

#### الإدارة الأساسية
- `/settings` - لوحة التحكم الرئيسية
- `/ban` - حظر مستخدم من المجموعة
- `/unban` - إلغاء حظر مستخدم
- `/mute` - كتم مستخدم
- `/unmute` - إلغاء كتم مستخدم

#### نظام التحذيرات
- `/warn` - إعطاء تحذير لمستخدم
- `/unwarn` - إزالة تحذير من مستخدم
- `/warnings` - عرض عدد التحذيرات

#### إدارة المحتوى
- `/addword كلمة` - إضافة كلمة محظورة
- `/removeword كلمة` - إزالة كلمة محظورة
- `/setwelcome رسالة` - تعيين رسالة ترحيب
- `/delwelcome` - حذف رسالة الترحيب

### أوامر الجميع
- `/start` - بدء التفاعل مع البوت
- `/help` - عرض قائمة المساعدة
- `/warnings` - عرض تحذيراتك الشخصية
- `/gbt` - استخدام الذكاء الاصطناعي

## 🚀 التثبيت والتشغيل

### المتطلبات
```bash
pip install pyTelegramBotAPI
pip install opennsfw2
pip install pillow
pip install yt-dlp
pip install requests
```

### التشغيل
```bash
python ste_new.py
```

## ⚙️ الإعدادات

قم بتعديل ملف `config.py` لتغيير:
- توكن البوت
- مفتاح YouTube API
- معلومات القناة
- معرف المطور

## 📊 الفرق بين النسخة القديمة والجديدة

| الميزة | النسخة القديمة | النسخة الجديدة |
|--------|----------------|----------------|
| البنية | ملف واحد كبير (2920 سطر) | وحدات منظمة ومتخصصة |
| لوحة التحكم | أوامر متفرقة | لوحة تفاعلية بالأزرار |
| نظام التحذيرات | غير موجود | نظام متكامل مع كتم تلقائي |
| سهولة التطوير | صعبة | سهلة جداً |
| الصيانة | معقدة | بسيطة ومنظمة |

## 🔄 الترحيل من النسخة القديمة

جميع ملفات البيانات متوافقة مع النسخة الجديدة:
- `banned_words.json`
- `welcome.json`
- `detection_status.json`
- `report_groups.json`
- وجميع الملفات الأخرى

لا حاجة لأي تعديلات على البيانات الموجودة!

## 📝 ملاحظات مهمة

1. **الملف الأصلي محفوظ**: الملف الأصلي `ste.py` لم يتم تعديله، النسخة الجديدة في `ste_new.py`
2. **التوافق الكامل**: جميع الميزات الأصلية تعمل بنفس الطريقة
3. **قابلية التطوير**: البنية الجديدة تسهل إضافة ميزات جديدة
4. **الأداء**: لا يوجد فرق في الأداء، فقط تنظيم أفضل

## 🆘 الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع المطور.

## 📜 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

**تم التطوير بواسطة:** Manus AI  
**تاريخ الإصدار:** نوفمبر 2025  
**الإصدار:** 2.0
